server {
    listen 5173;
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend:3000;  # Port interne du backend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}